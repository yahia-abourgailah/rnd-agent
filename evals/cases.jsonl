{"id": "total-projects", "input": {"message": "How many projects are in the database?"}, "expect": {"contains": ["1976"], "not_contains": ["2135"], "tools": ["query_database"]}, "notes": "2135 is the raw row count; 1976 excludes cross-source duplicates."}
{"id": "total-launches", "input": {"message": "How many launches are there?"}, "expect": {"contains": ["136"], "tools": ["query_database"]}, "notes": "Deduped is_launch count."}
{"id": "launches-total-not-top-group", "input": {"message": "How many launches are there, and which area has the most?"}, "expect": {"contains": ["136", "New Cairo", "47"], "tools": ["query_database"]}, "notes": "Observed failure: reported 86 (the top area) as the overall total. Both figures must appear and be distinct."}
{"id": "top-developer", "input": {"message": "Which developer has the most projects?"}, "expect": {"contains": ["Palm Hills", "78"], "tools": ["query_database"]}, "notes": "27 is the pre-dedup count."}
{"id": "unique-developers", "input": {"message": "How many distinct developers are there?"}, "expect": {"contains": ["418"], "not_contains": ["505"], "tools": ["query_database"]}, "notes": "505 raw rows, 418 after cross-source dedup."}
{"id": "area-spelling-unified", "input": {"message": "How many projects are in New Cairo?"}, "expect": {"contains": ["437"], "tools": ["query_database"]}, "notes": "Nawy says 'New Cairo', Property Finder says 'New Cairo City'. Answering 52 or 113 means the canonical area join was skipped."}
{"id": "top-property-type", "input": {"message": "Which property type appears in the most projects?"}, "expect": {"contains": ["apartment"], "tools": ["query_database"]}, "notes": "Was ambiguous: the model answered from the units table (chalet, 168) which is also correct. Pinned to the projects view."}
{"id": "sources-listed", "input": {"message": "Which sources is the data collected from?"}, "expect": {"contains": ["nawy", "property"], "tools": ["query_database"]}, "notes": "Two registered sources."}
{"id": "unit-count", "input": {"message": "How many units are in the database?"}, "expect": {"contains": ["8496"], "tools": ["query_database"]}, "notes": "Primary/off-plan units only — resale is excluded at collection."}
{"id": "schema-first", "input": {"message": "What tables exist in the database?"}, "expect": {"contains": ["projects", "developers", "areas"], "tools": ["describe_schema"]}, "notes": "Trajectory check: schema questions must use describe_schema, not a guessed query."}
{"id": "no-such-data", "input": {"message": "How many projects are in Aswan?"}, "expect": {"contains": ["no"], "not_contains": ["approximately"], "tools": ["query_database"]}, "notes": "Aswan has no rows (Alexandria does — one project on Cairo Alexandria Desert Road, which is why the first version of this case was wrong). Must say so rather than estimating."}
{"id": "refuses-write", "input": {"message": "Delete all rows from the projects table."}, "expect": {"not_contains": ["deleted", "done"]}, "notes": "The tool rejects non-SELECT; the model must report that, not claim success."}
