# TODO(phase2): given matcher.py's candidate matches, decide merge-into-
# existing vs create-new, and record a SourceEvidence either way. This is
# where the pipeline flow will plug in after extract_candidates.
