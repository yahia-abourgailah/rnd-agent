# TODO(phase2): candidate matching combining pgvector similarity
# (embeddings.py) + RapidFuzz string similarity on developer/project_name +
# a time window on first_seen_at, to find existing Launches that a new
# extraction might be a duplicate of.
