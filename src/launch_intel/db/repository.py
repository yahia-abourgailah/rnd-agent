# TODO(phase-later): save_launch(launch: Launch), attach_evidence(evidence:
# SourceEvidence), and query helpers for the API/dashboard layer. This is
# where dedup.resolver's merge-or-create decision gets persisted.
