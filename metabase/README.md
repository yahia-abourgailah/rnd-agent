TODO(phase-later): exported Metabase dashboard config / saved questions go
here once the API and DB layers are wired up. Metabase itself is configured
via its own UI against the Postgres instance in docker-compose.yml — no
code needed, just exports checked in for reproducibility.
