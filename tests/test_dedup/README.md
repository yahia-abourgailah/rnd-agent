TODO(phase2): dedup tests go here once embeddings.py/matcher.py/resolver.py
are implemented — this is called out in the spec as the most important test
suite, since dedup bugs (false merges/false splits) are the hardest to catch
after the fact.
